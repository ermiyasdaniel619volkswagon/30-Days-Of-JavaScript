//  array constractor is used to create an array
const arr = Array(); // create an array using Array ()  method
console.log(arr);
/// array with limited values
const TwoEmptyValues = Array(2);
console.log(TwoEmptyValues);
/// fill  valus in the above array
const SexValue = TwoEmptyValues.fill("Male", "Female");
console.log(SexValue);
